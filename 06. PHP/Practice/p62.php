<?php

$string = "Hello World";

// to find positioning
// method: substr( stringName, starting index, length or how many characters we want from the starting index )
$result = substr( $string, 5, 5 );

echo $result;