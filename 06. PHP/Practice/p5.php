<?php

$name = "Faizul";
$age = 16;
$result = True;
// with output, if we want to know additional info about the variable, we use var_dump() ->
var_dump( $name ); // it will show the type (string(with length)) and value
var_dump( $age ); // it will show the type (int(value))
var_dump( $result ); // it will show the type (bool(value)) -> value is true or false
